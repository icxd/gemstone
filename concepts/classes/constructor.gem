class Foo {
    public var bar: int;
    public anonymous const baz: char*;
    private const foo: int = 123;
}

function main(argc: int, argv: char**) -> int {
    const foo: Foo = new Foo(bar: 123, "baz");
    return 0;
}